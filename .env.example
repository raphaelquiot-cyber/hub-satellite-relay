# Port d'écoute du relais (défaut : 4000)
PORT=4000

# En production (VPS) : mettre wss:// et configurer TLS via nginx reverse proxy
# Le relais lui-même écoute en ws:// — TLS est géré par le reverse proxy
